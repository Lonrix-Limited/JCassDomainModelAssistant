# docs/framework/ — placeholder

**Filled by session S7.**

Will hold the framework API reference, generated from the XML documentation shipped alongside the
assemblies in [`../../refs/`](../../refs/) — every public type, method signature and parameter a
domain model can call.

This is the most load-bearing document in the repository. It is what an assistant checks a call
against before writing it, and **a framework call that does not appear here is the signal to stop
and ask support@lonrix.com** rather than to invent one.
