# examples/ — placeholder

**Filled by session S10.**

Will hold small, focused, compiling examples of individual patterns — one idea each, in contrast to
[`../reference-model/DomainModelSample/`](../reference-model/DomainModelSample/), which is a whole
small model.

Examples may carry their own `.csproj` files. That is safe here for one reason and it is worth
knowing: **the Assistant is never uploaded to the Debug Model page.** Only the engineer's own model
folder is, and it is uploaded whole, rooted at its single `.csproj`.
