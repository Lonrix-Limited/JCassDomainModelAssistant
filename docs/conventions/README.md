# docs/conventions/ — placeholder

**Filled by session S8.**

Will hold the conventions that fail *silently* — which is the whole reason this repository exists.
An error is self-correcting; silence is not. Expect the four-name rule, where tunable numbers live,
which names must match across C# and the bundle, and what a `supporting/` side-car CSV is for.

Until then, see [`../../reference-model/DomainModelSample/README.md`](../../reference-model/DomainModelSample/README.md)
sections 3, 7 and 8.
