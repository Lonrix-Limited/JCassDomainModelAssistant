# docs/patterns/ — placeholder

**Not written yet.**

Will hold the canonical shape of each recurring piece of a real model — the element class, the
factory, the trigger, the incrementer and resetter, strategy generation, treatment costing and
budget-category splits — mined from the domain models already in production.

Until then, see [`../../reference-model/DomainModelSample/Objects/`](../../reference-model/DomainModelSample/Objects/),
which is a small worked example of most of them.
